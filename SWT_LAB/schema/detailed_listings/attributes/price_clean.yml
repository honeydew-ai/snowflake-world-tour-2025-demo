type: calculated_attribute
sql: |-
  CAST(REPLACE(REPLACE(detailed_listings.price, '$', ''), ',', '') AS FLOAT)
entity: detailed_listings
name: price_clean
description: |-
  The Price_clean column represents the cleaned and standardized price of a listing, likely removing any unnecessary characters or formatting to provide a numerical value for analysis or comparison.
metadata:
  - name: honeydew_ai
    metadata:
      - name: synonyms
        value: |-
          clean_price,price_amount,price_figure,price_number,price_value
datatype: float
