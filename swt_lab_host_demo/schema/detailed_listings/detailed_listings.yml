type: entity
name: detailed_listings
keys:
  - id
key_dataset: detailed_listings
relations:
  - rel_type: many-to-one
    target_entity: currency_for_city
    connection:
      - src_field: listing_city
        target_field: city
    rel_join_type: left
    cross_filtering: both
  - rel_type: many-to-one
    target_entity: dim_host
    connection:
      - src_field: host_id
        target_field: host_id
    rel_join_type: left
    cross_filtering: both
  - rel_type: many-to-one
    target_entity: dim_listings_review
    connection:
      - src_field: id
        target_field: listing_id
    rel_join_type: left
    cross_filtering: both
